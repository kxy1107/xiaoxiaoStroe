<!--轮播图-->
<swiper indicator-dots="{{indicatorDots}}" autoplay="{{autoplay}}" interval="{{interval}}" duration="{{duration}}" bindchange="swiperChange">
  <block wx:for="{{imgUrls}}">
    <swiper-item>
      <image src="{{item}}" class="swiper-image" bindtap="swiperClick" />
    </swiper-item>
  </block>
</swiper>

<view class="separate"></view>
<!--导航-->
<view class="nav">
  <view wx:for="{{navs}}" class="nav-item">
    <view class="ui-flex-ver ui-flex-center " catchtap="navClick" data-type="{{item.text}}">
      <image class="nav-img" src="{{item.icon}}"></image>
      <text class="nav-text">{{item.text}}</text>
    </view>
  </view>
</view>

<!--最热-->
<view class="category-title">
  <view>
    <image class="small-icon-img" src="../../img/volume_small.png"></image>
  </view>
  <view>
    <text class="small-text">最热</text>
  </view>
</view>

<view class="item-container">
  <block wx:for="{{shopList}}" wx:key="id">
    <view class="shop-item" catchtap="itemClick" data-id="{{item.shopID}}">
      <image class="huawei-item-img" src="{{item.imgUrl}}"></image>
      <view class="shop-info">
        <view class="huawei-item-title">
          <text class="shop-title">{{item.shopName}}</text>
        </view>
        <view>
          <text class="item-price">{{item.price}}</text>
          <text class="item-sales">{{item.sales}}</text>
        </view>
      </view>
    </view>
  </block>
</view>

<!--最新-->

<view class="category-title">
  <view>
    <image class="small-icon-img" src="../../img/new_small.png"></image>
  </view>
  <view>
    <text class="small-text">最新</text>
  </view>
</view>

 <view class="item-container">
      <block wx:for="{{shopList}}" wx:key="id">
        <view class="shop-item" catchtap = "itemClick" data-id = "{{item.shopID}}">
          <image class="huawei-item-img" src="{{item.imgUrl}}"></image>
          <view class = "shop-info">
            <view class="huawei-item-title">
              <text class = "shop-title">{{item.shopName}}</text>
            </view>
            <view>
              <text class="item-price">{{item.price}}</text>
              <text class="item-sales">{{item.sales}}</text>
            </view>
          </view>
        </view>
      </block>
    </view>